import type { FeatureSection } from "@/config/homepage";
type Props = { id: string; section: FeatureSection; visualTitle: string; visualItems: readonly string[]; reverse?: boolean };
export function FeaturePanel({ id, section, visualTitle, visualItems, reverse = false }: Props) { return <section className={`ld-section ld-feature ${reverse ? "ld-feature--reverse" : ""}`} aria-labelledby={`${id}-title`}><div><p className="ld-eyebrow">{section.eyebrow}</p><h2 id={`${id}-title`}>{section.title}</h2><p>{section.description}</p><ul className="ld-check-list">{section.items.map((item)=><li key={item}>{item}</li>)}</ul><a className="ld-button ld-button--primary" href={section.cta.href}>{section.cta.label}</a></div><div className="ld-wire-card" role="img" aria-label={visualTitle}><h3>{visualTitle}</h3>{visualItems.map((item)=><div className="ld-wire-row" key={item}>{item}</div>)}</div></section>; }

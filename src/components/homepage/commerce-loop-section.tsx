import { homepageConfig } from "@/config/homepage"; import { SectionHeading } from "./section-heading";
export function CommerceLoopSection() { const l = homepageConfig.loop; return <section className="ld-section" aria-labelledby="loop-title"><SectionHeading id="loop-title" eyebrow={l.eyebrow} title={l.title} description={l.description} align="center"/><ol className="ld-loop">{l.stages.map((stage, index)=><li key={`${stage}-${index}`} data-side={index < 10 ? "customer" : "merchant"}><span>{stage}</span></li>)}</ol><p className="ld-note">Customer-facing stages lead into merchant-facing operations, then back into repeat purchase.</p></section>; }

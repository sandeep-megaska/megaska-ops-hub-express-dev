import type { PromotionExecutionReadModel, PromotionAdminTone } from "../../../services/promotions/compiler-admin-read-model.server";

export function toneClasses(tone: PromotionAdminTone) { return tone === "success" ? "border-green-200 bg-green-50 text-green-800" : tone === "warning" ? "border-amber-200 bg-amber-50 text-amber-800" : tone === "critical" ? "border-red-200 bg-red-50 text-red-800" : tone === "info" ? "border-blue-200 bg-blue-50 text-blue-800" : "border-gray-200 bg-gray-50 text-gray-700"; }
export default function PromotionExecutionStatus({ model, ruleStatus, saved, publication, sync }: { model: PromotionExecutionReadModel; ruleStatus: string; saved?: string; publication?: string; sync?: string }) { const current = model.currentCompilation; const publicationLabel = sync === "SYNCED" ? "SYNCED" : publication === "failed" ? "FAILED" : current?.status === "READY" ? "Ready for automatic synchronization" : "Not configured"; return <section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Compilation status</h2><div className="mt-4 grid gap-3 text-sm md:grid-cols-2"><p><span className="text-gray-500">Rule status:</span> {ruleStatus}</p><p><span className="text-gray-500">Compilation status:</span> <span className={`rounded-full border px-2 py-0.5 text-xs font-semibold ${toneClasses(model.tone)}`}>{model.label}</span></p><p><span className="text-gray-500">Current version:</span> {current?.version ?? "—"}</p><p><span className="text-gray-500">Qualifying product count:</span> {current?.membershipCount ?? "—"}</p><p><span className="text-gray-500">Last compiled time:</span> {current?.compiledAt ? new Date(current.compiledAt).toLocaleString("en-IN") : "—"}</p><p><span className="text-gray-500">Shopify synchronization:</span> {publicationLabel}</p></div>{saved ? <p className="mt-3 rounded-lg border border-green-200 bg-green-50 p-3 text-sm text-green-800">Saved. Compilation: {model.latestAttempt?.status ?? model.currentCompilation?.status ?? "—"}. Shopify synchronization: {publicationLabel}.</p> : null}{publication === "failed" ? <p className="mt-3 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-800">Saved, but automatic publication did not complete. Review the compilation and Shopify synchronization status before considering this promotion published.</p> : null}{model.latestAttempt?.safeErrorMessage ? <p className="mt-3 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-800">{model.latestAttempt.safeErrorMessage}</p> : null}</section>; }

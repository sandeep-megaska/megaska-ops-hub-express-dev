"use client";
import { useEffect, useState } from "react";
type Review={id:string;status:string;rating:number;title:string|null;body:string|null;customerDisplayName:string;verifiedPurchase:boolean;productTitleSnapshot:string;submittedAt:string;rejectionReason:string|null};type Data={reviews:Review[];counts:{pending:number;published:number;rejected:number;total:number}};
export default function ReviewModerationClient({shop}:{shop:string}){const [status,setStatus]=useState("PENDING_MODERATION"),[search,setSearch]=useState(""),[data,setData]=useState<Data|null>(null),[selected,setSelected]=useState<Review|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[reason,setReason]=useState("");const load=async()=>{setLoading(true);try{const r=await fetch(`/api/admin/reviews?shop=${encodeURIComponent(shop)}&status=${status}&search=${encodeURIComponent(search)}`,{cache:"no-store"}),b=await r.json();if(!r.ok)throw new Error(b.error||"Could not load reviews.");setData(b)}catch(e){setError(e instanceof Error?e.message:"Could not load reviews.")}finally{setLoading(false)}};useEffect(()=>{const t=setTimeout(load,200);return()=>clearTimeout(t)},[status,search,shop]);const act=async(action:string)=>{if(!selected||busy||!window.confirm(`${action.toLowerCase()} this review?`))return;setBusy(true);try{const r=await fetch(`/api/admin/reviews/${selected.id}/moderate?shop=${encodeURIComponent(shop)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,rejectionReason:reason})});if(!r.ok)throw new Error("Moderation failed.");setReason("");await load()}catch(e){setError(e instanceof Error?e.message:"Moderation failed.")}finally{setBusy(false)}};return <main style={{padding:24,display:"grid",gap:16}}><header><h1>Reviews</h1><p>Moderate verified customer feedback and keep your storefront trustworthy.</p></header>{data&&<section aria-label="Review status summary" style={{display:"grid",gridTemplateColumns:"repeat(4,minmax(110px,1fr))",gap:8}}>{Object.entries(data.counts).map(([label,value])=><div key={label} style={{padding:12,border:"1px solid #ddd",borderRadius:8}}><strong>{value}</strong><br/><small>{label}</small></div>)}</section>}<div><label>Status <select value={status} onChange={e=>setStatus(e.target.value)}><option value="PENDING_MODERATION">Pending</option><option value="PUBLISHED">Published</option><option value="REJECTED">Rejected</option><option value="ALL">All</option></select></label>{" "}<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search product, order, or customer" aria-label="Search reviews" /></div>{error&&<p role="alert">{error} <button onClick={load}>Try again</button></p>}{loading?<div aria-label="Loading reviews">Loading review cards…</div>:data?.reviews.length===0?<p>No reviews match these filters. Try a different status or search.</p>:<div style={{display:"grid",gridTemplateColumns:"minmax(0,2fr) minmax(280px,1fr)",gap:16}}><div>{data?.reviews.map(r=><button type="button" key={r.id} onClick={()=>setSelected(r)} style={{display:"block",width:"100%",textAlign:"left",padding:12,marginBottom:8,border:"1px solid #ddd",background:selected?.id===r.id?"#f0f7ff":"white"}}><strong>{"★".repeat(r.rating)} {r.title||"Untitled review"}</strong><br/>{(r.body||"").slice(0,180)}<br/><small>{r.customerDisplayName} {r.verifiedPurchase?"• Verified purchase":""} • {r.productTitleSnapshot} • {r.status}</small></button>)}</div>{selected&&<aside style={{border:"1px solid #ddd",padding:16}}><h2>{selected.title||"Untitled review"}</h2><p>{"★".repeat(selected.rating)}</p><p style={{whiteSpace:"pre-wrap"}}>{selected.body||"No written review."}</p><p><b>Customer:</b> {selected.customerDisplayName} {selected.verifiedPurchase&&"(Verified purchase)"}</p>{selected.rejectionReason&&<p><b>Internal rejection reason:</b> {selected.rejectionReason}</p>}<textarea value={reason} onChange={e=>setReason(e.target.value)} maxLength={500} placeholder="Optional internal rejection reason" aria-label="Optional internal rejection reason"/><div style={{display:"flex",gap:8,marginTop:8}}>{selected.status!=="PUBLISHED"&&<button disabled={busy} onClick={()=>act("PUBLISH")}>Publish</button>}{selected.status!=="REJECTED"&&<button disabled={busy} onClick={()=>act("REJECT")}>Reject</button>}{selected.status!=="PENDING_MODERATION"&&<button disabled={busy} onClick={()=>act("UNPUBLISH")}>Unpublish</button>}</div></aside>}</div>}</main>}

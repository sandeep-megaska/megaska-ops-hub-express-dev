import type { NextRequest } from "next/server";import { finalizeReviewMediaUpload } from "../../../../../services/reviews/review-media-upload";import { allowedOrigin,parseJson,rateLimit,reply,requireReviewProxy } from "../../../../../services/reviews/review-submission-http";
export async function POST(req:NextRequest){if(!allowedOrigin(req))return reply({ok:false,errorCode:"REVIEW_MEDIA_TOKEN_INVALID"},403);const shop=await requireReviewProxy(req),body=await parseJson(req);if(!shop||!body)return reply({ok:false,errorCode:"REVIEW_MEDIA_TOKEN_INVALID"},401);if(!rateLimit(req,"review-media-finalize",20,900000))return reply({ok:false,errorCode:"RATE_LIMITED"},429);const result=await finalizeReviewMediaUpload({shopId:shop.id,token:body.token,mediaId:body.mediaId});return reply(result,result.ok?200:409)}export const dynamic="force-dynamic";

import { NextRequest,NextResponse } from "next/server";import { resolveAdminShopFromRequest,formatAdminShopResolutionError } from "../../../../../../services/shopify/admin-shop-context";import { resolveReviewAnalyticsRange } from "../../../../../../services/reviews/review-analytics-date-range";import { overview } from "../../../../../../services/reviews/review-analytics";
export const dynamic="force-dynamic";export async function GET(req:NextRequest){const context=await resolveAdminShopFromRequest(req);if(!context.shop?.id)return NextResponse.json({ok:false,error:formatAdminShopResolutionError(context)},{status:401});try{return NextResponse.json({ok:true,...await overview(context.shop.id,resolveReviewAnalyticsRange(req.nextUrl.searchParams))},{headers:{"Cache-Control":"no-store"}})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Invalid analytics request."},{status:400})}}

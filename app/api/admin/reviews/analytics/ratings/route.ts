import { NextRequest,NextResponse } from "next/server";import { resolveAdminShopFromRequest,formatAdminShopResolutionError } from "../../../../../../services/shopify/admin-shop-context";import { resolveReviewAnalyticsRange } from "../../../../../../services/reviews/review-analytics-date-range";import { ratings } from "../../../../../../services/reviews/review-analytics";
export const dynamic="force-dynamic";export async function GET(req:NextRequest){const c=await resolveAdminShopFromRequest(req);if(!c.shop?.id)return NextResponse.json({ok:false,error:formatAdminShopResolutionError(c)},{status:401});try{return NextResponse.json({ok:true,...await ratings(c.shop.id,resolveReviewAnalyticsRange(req.nextUrl.searchParams))},{headers:{"Cache-Control":"no-store"}})}catch{return NextResponse.json({ok:false,error:"Invalid analytics range."},{status:400})}}

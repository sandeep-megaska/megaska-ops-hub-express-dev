import test from "node:test"; import assert from "node:assert/strict"; import { persistActionIdempotency, resolveActionIdempotency, resetActionIdempotencyForTest } from "./idempotency.ts";
const context={shopId:"s",shopDomain:"s.myshopify.com",customerProfileId:"c",sessionId:"sess",now:new Date("2026-01-01")}; const request={actionType:"CANCELLATION",orderId:"o",idempotencyKey:"k",payload:{reasonCode:"A"}} as const; const result={ok:true,actionType:"CANCELLATION",requestId:"r",statusCode:"OPEN",statusLabel:"Cancellation requested",customerMessage:"Submitted",orderId:"o",orderNumber:"#1",createdAt:"2026-01-01T00:00:00.000Z",nextAction:{type:"REFRESH_DASHBOARD",url:null}} as const;
test("replays same scoped idempotency key and rejects changed payload",async()=>{resetActionIdempotencyForTest(); assert.equal(await resolveActionIdempotency({context,request,payload:{reasonCode:"A"}}),null); await persistActionIdempotency({context,request,payload:{reasonCode:"A"},result}); assert.equal((await resolveActionIdempotency({context,request,payload:{reasonCode:"A"}}))?.requestId,"r"); await assert.rejects(resolveActionIdempotency({context,request,payload:{reasonCode:"B"}}),/different request/);});
